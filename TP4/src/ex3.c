#include <stdio.h>
#include <string.h>
#include <mpi.h>
#include <stdint.h>
#include <stdlib.h>

#define N 10

//https://www.mpich.org/static/docs/v3.2/www3/MPI_Barrier.html

int main(int argc, char** argv)
{
	/*
	int my_rank;
	int nbProc;
	MPI_Status status;

	MPI_Init(&argc, &argv);

	MPI_Comm_rank(MPI_COMM_WORLD, &my_rank);
	MPI_Comm_size(MPI_COMM_WORLD, &nbProc);

    uint32_t timing = 0;



	printf("process %d : our sum is %p\n", my_rank, &sum);

	MPI_Finalize();
	*/
	return EXIT_SUCCESS;

}